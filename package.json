{
  "name": "esy-ci-pipelines",
  "version": "0.1.0",
  "description": "Simple test project for a pipeline build", "license": "MIT",
  "esy": {
    "build": "touch #{self.target_dir / self.name}.txt",
    "install": "cp #{self.target_dir / self.name}.txt #{self.doc / self.name}.txt"
  },
  "dependencies": {
  }
}
